subprojects {
    def loader = name.substring(0, name.indexOf('-'))
    def mc = name.substring(name.indexOf('-') + 1)
    if (loader == 'fabric' && !mc.startsWith('1.')) {
        ext['fabric.loom.disableObfuscation'] = 'true'
    }
    apply plugin: 'mc-base'
    apply plugin: "mc-${loader}"
}
